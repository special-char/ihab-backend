version: '3.7'

services:
  nginx:
    build: ./nginx
    ports:
      - 80:80
    depends_on:
      - api
    networks:
      - webnet
  api:
    build: ./
    ports:
      - 3000:3000
    env_file:
      - .env
    networks:
      - webnet
networks:
  webnet:
